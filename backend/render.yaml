services:
  - type: web
    name: skin-cancer-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: APP_ENV
        value: production
      - key: MODEL_TYPE
        value: simple_cnn
      - key: MODEL_CHECKPOINT
        value: models/skin_lesion_cnn_baseline.pth
      - key: MAX_UPLOAD_SIZE_MB
        value: "10"
      - key: ALLOWED_ORIGINS
        sync: false
